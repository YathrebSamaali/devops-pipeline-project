apiVersion: v1
kind: Service
metadata:
  name: spring-boot-service
spec:
  type: NodePort
  selector:
    app: my-spring-boot-app
  ports:
    - protocol: TCP
      port: 8087
      targetPort: 8087
      nodePort: 30000
